import { TagsPropPipe } from './tags-prop.pipe';

describe('TagsPropPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TagsPropPipe();
    expect(pipe).toBeTruthy();
  });
});
