{
  "name": "osm-pt-ngx-leaflet",
  "scripts": {
    "build": "ng build",
    "heroku-postbuild": "echo This runs after the build",
    "heroku-prebuild": "echo This runs before Heroku installs your dependencies"
  },
  "env": {
    "NPM_CONFIG_PRODUCTION": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "stack": "heroku-18"
}
